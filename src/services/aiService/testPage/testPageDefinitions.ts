/**
 * Definitions for TestPage
 */
export const DEFINITIONS = {
  selectors: {
    textInput: ['div[contenteditable="true"].ql-editor'],
    sendButton: [
      // test page
      'form button[type="submit"]',
    ],
  },
  popupPlacement: {
    alignOffset: -10,
    sideOffset: 22,
  },
}
